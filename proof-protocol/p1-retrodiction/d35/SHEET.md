# D35 — blind rating sheet: `move_kind`, one field, 17 items

Generated by `d35/build_sheet.py` from the case files. Do not hand-edit.

You are rating ONE property of 17 proof strategies. Do not use any tools: no file
reads, no web access, no code execution, no searching. Answer from the text below
plus your own knowledge.

**The question, for each item:** is the move a **FORMULA** that is carried out, or
an **EXISTENCE THEOREM** that asserts a good object exists?

Answer with exactly one of:

* `FORMULA` — the move is computed or written down; nothing has to be shown to exist first.
* `EXISTENCE-THEOREM` — a legal move requires some object to be shown to exist.
* `NO MOVE RECORDED` — the text does not describe a move at all. This is a legitimate
  answer and you should use it rather than guess.

Give a one-line reason for each. The reasons are read before the codes are scored.
Do not try to identify which project this is or what the expected answers are.

---

**1. Roth's theorem in [N] (Bloom-Sisask 2023, the Kelley-Meka exposition and refinement)**

> Hoelder lifting, unbalancing, dependent random choice, almost-periodicity -- Kelley-Meka's own five steps, with the ad hoc iteration over high-dimensional progressions replaced by classical Bohr set machinery.

**2. Kadison-Singer (Weaver KS_2)**

> Random partition.

**3. Cap set problem**

> Large Fourier coefficient, then restrict to a hyperplane.

**4. Vinogradov mean value theorem**

> p-adic congruencing iteration.

**5. Spencer set discrepancy (six standard deviations)**

> Uniform random colouring.

**6. PCP theorem (weak case)**

> k-fold parallel repetition.

**7. Sphere packing in dimensions 8 and 24**

> NO MOVE SLOT RECORDED. The rest of the board reads: name = Cohn-Elkies 2003; S = An auxiliary function f with f <= 0 for |x| >= r, f-hat >= 0, f(0) = f-hat(0).; T = A density bound.

**8. Parallel repetition**

> NO MOVE SLOT RECORDED. The rest of the board reads: S = A two-prover game under k-fold product.; I = Conjectured: val(G^k) = val(G)^k.

**9. Sensitivity conjecture**

> Subcube refinement.

**10. Roth's theorem in [N] (Kelley-Meka 2023)**

> NO MOVE SLOT RECORDED. The rest of the board reads: name = density increment; S = A 3AP-free subset of [N].; I = Density increment.

**11. Small prime gaps**

> Selberg's construction lambda(d) = mu(d)G(log d / log R) -- an EXPLICIT FORMULA, not an existence theorem.

**12. Bounded prime gaps via smooth moduli (Zhang 2013)**

> Selberg's explicit formula, unchanged

**13. Shannon capacity of C5**

> NO MOVE SLOT RECORDED. The rest of the board reads: S = A graph under the strong product.; I = Fractional clique cover alpha* (multiplicative).; T = Theta <= alpha*.

**14. Erdos distinct distances**

> Count isosceles triangles, or circle incidences.

**15. Geometrization / Ricci flow**

> NO MOVE SLOT RECORDED. The rest of the board reads: S = A metric under Ricci flow (Hamilton).; I = Curvature pinching via the maximum principle.; T = A round or decomposed limit.

**16. Finite-field Kakeya**

> Bush / hairbrush incidence counting.

**17. Kneser conjecture**

> Combinatorial and inductive arguments.

---

Finish with a table: item number, your code, and your one-line reason.
